# Example environment variables for Scheduly (frontend build)

# Public base URL used for share links (e.g., https://scheduly.example.com)
BASE_URL=

# API base URL (leave empty for static/on-memory mode)
# Example: https://api.scheduly.example.com
API_BASE_URL=

# CORS allowed origins (comma-separated). Leave empty to disable CORS handling in static mode.
# Example: https://scheduly.example.com,https://admin.scheduly.example.com
CORS_ALLOWED_ORIGINS=

# Build mode (injected by npm scripts typically)
# NODE_ENV=production
